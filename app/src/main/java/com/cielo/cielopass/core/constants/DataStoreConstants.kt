package com.cielo.cielopass.core.constants

object DataStoreConstants {
    const val CREDENTIALS_FILE_NAME = "cielo_credentials.pb"
    const val ERR_EMPTY_ENCRYPTED_STREAM = "Encrypted bytes stream is empty"
    const val ERR_DECRYPT_CREDENTIALS_PROTO = "Cannot read or decrypt credentials proto."
}
